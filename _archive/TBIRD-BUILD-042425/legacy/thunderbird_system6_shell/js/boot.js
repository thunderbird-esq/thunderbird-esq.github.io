import {launch} from './apps.js';import {WM} from './windowManager.js';const desktop=document.getElementById('desktop');desktop.addEventListener('click',e=>{if(e.target.classList.contains('desktop-icon')){document.querySelectorAll('.desktop-icon').forEach(i=>i.classList.remove('selected'));e.target.classList.add('selected');}else{document.querySelectorAll('.desktop-icon.selected').forEach(i=>i.classList.remove('selected'));}});
desktop.addEventListener('dblclick',e=>{if(e.target.dataset.app)launch(e.target.dataset.app);});
const structure={apple:['About THUNDERBIRD…'],file:['Close Window','Put Away'],edit:['Cut','Copy','Paste'],special:['Empty Trash…']};document.querySelectorAll('#menubar [data-menu]').forEach(el=>{el.addEventListener('click',e=>{document.querySelectorAll('.menu-popup').forEach(p=>p.remove());const key=el.dataset.menu;const pop=document.createElement('ul');pop.className='menu-popup';structure[key].forEach(txt=>{const li=document.createElement('li');li.textContent=txt;li.onclick=()=>{if(txt.startsWith('About'))launch('about');pop.remove();};pop.appendChild(li);});const r=el.getBoundingClientRect();pop.style.left=r.left+'px';document.body.appendChild(pop);});});
window.addEventListener('click',e=>{if(!e.target.closest('.menu-popup'))document.querySelectorAll('.menu-popup').forEach(p=>p.remove());});
window.addEventListener('DOMContentLoaded',()=>launch('about'));